import BaseLayout from './base.layout.vue';
import ToolLayout from './tool.layout.vue';
import ToolWideLayout from './tool-wide.layout.vue';

export const layouts = {
  base: BaseLayout,
  toolLayout: ToolLayout,
  toolWideLayout: ToolWideLayout,
};
